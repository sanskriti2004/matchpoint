name: CI/CD Pipeline

on:
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Install Dependencies (Server)
        run: |
          cd server
          npm install

      # Add Linting or Testing steps here
      # - name: Run Tests
      #   run: npm test

      - name: Build Docker Image
        run: |
          docker build -t my-resume-backend ./server
          # Login and Push to DockerHub logic would go here
